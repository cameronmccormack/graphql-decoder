{
  "name": "graphql-decoder",
  "version": "1.0.1",
  "description": "A utility for decoding a URL GraphQL request to its formatted constituent parts.",
  "main": "index.js",
  "bin": "index.js",
  "type": "module",
  "author": "Cameron McCormack",
  "license": "ISC",
  "dependencies": {
    "prettier": "^3.0.3"
  },
  "scripts": {
    "lint": "prettier . --check",
    "lint:fix": "prettier . --write"
  }
}
